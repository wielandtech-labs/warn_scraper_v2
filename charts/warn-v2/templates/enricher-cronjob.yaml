{{- if .Values.enricher.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "warn-v2.fullname" . }}-enricher
  labels:
    {{- include "warn-v2.labels" . | nindent 4 }}
spec:
  schedule: {{ .Values.enricher.schedule | quote }}
  # Forbid: never overlap runs. Critical when the provider image is used —
  # the provider locks the seat (~1h) on a second concurrent session.
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      backoffLimit: 1
      template:
        metadata:
          labels:
            {{- include "warn-v2.labels" . | nindent 12 }}
            app.kubernetes.io/component: enricher
        spec:
          restartPolicy: Never
          {{- with .Values.enricher.imagePullSecrets }}
          imagePullSecrets:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          containers:
            - name: enricher
              image: {{ include "warn-v2.enricherImage" . }}
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              args:
                - enrich
                - --limit
                - {{ .Values.enricher.limit | quote }}
                - --recent-years
                - {{ .Values.enricher.recentYears | quote }}
                {{- if .Values.enricher.recentLimit }}
                - --recent-limit
                - {{ .Values.enricher.recentLimit | quote }}
                {{- end }}
                {{- if .Values.enricher.tiers }}
                - --tiers
                - {{ .Values.enricher.tiers | quote }}
                {{- end }}
              env:
                {{- include "warn-v2.envSecrets" . | nindent 16 }}
                {{- if .Values.enricher.providerImage }}
                {{- if .Values.enricher.providerModule }}
                # Usually baked into the provider image's ENV; set here to override.
                - name: ENRICHMENT_PROVIDER_MODULE
                  value: {{ .Values.enricher.providerModule | quote }}
                {{- end }}
                - name: PROVIDER_USERNAME
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.dnb.secretName }}
                      key: {{ .Values.dnb.usernameKey }}
                - name: PROVIDER_PASSWORD
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.dnb.secretName }}
                      key: {{ .Values.dnb.passwordKey }}
                {{- if .Values.providerStorage.enabled }}
                - name: PROVIDER_STATE_PATH
                  value: /var/provider-profile/profile
                {{- end }}
                {{- if .Values.enricher.maxPerRun }}
                - name: PROVIDER_MAX_PER_RUN
                  value: {{ .Values.enricher.maxPerRun | quote }}
                {{- end }}
                {{- if .Values.enricher.maxPerDay }}
                - name: PROVIDER_MAX_PER_DAY
                  value: {{ .Values.enricher.maxPerDay | quote }}
                {{- end }}
                {{- end }}
              {{- if and .Values.enricher.providerImage .Values.providerStorage.enabled }}
              volumeMounts:
                - name: provider-profile
                  mountPath: /var/provider-profile
              {{- end }}
              resources:
                {{- toYaml .Values.enricher.resources | nindent 16 }}
          {{- if and .Values.enricher.providerImage .Values.providerStorage.enabled }}
          volumes:
            - name: provider-profile
              persistentVolumeClaim:
                claimName: {{ include "warn-v2.fullname" . }}-provider-profile
          {{- end }}
{{- end }}
