# State Data-Quality Audit

Living triage of per-jurisdiction WARN data quality. The table is **generated**,
not hand-maintained — regenerate it from a trusted environment:

```bash
# In-cluster, once the image containing the `audit` command is deployed
# (uses prod DATABASE_URL via secretKeyRef — no credentials leave the cluster):
warn-v2 audit --markdown
```

Paste the output between the GENERATED TABLE markers below, then hand-annotate the
**Source notes** section with Part-B findings (archive URLs, source caveats).

The audit logic lives in `warn_v2/scripts/audit.py` (`warn-v2 audit`). Everything in
the generated table is derived from the DB in a single pass; the **Source notes**
and **Backlog** sections are curated by hand from live-source inspection.

## Rubric

Each jurisdiction is scored on:

1. **Field completeness** — % non-null per model field the source could supply:
   `effective_date`, `layoff_count`, `closure_type`, `address`, `raw_notice_url`,
   `pdf_path`, `location_id`, company `naics_code`.
2. **PDF coverage** — for direct-PDF states (`raw_notice_url_is_pdf=True`): % of
   notices with a stored `pdf_path`, and whether the source URLs still resolve.
3. **Backfill / year completeness** — per-year notice counts; empty interior years
   are flagged; check whether an archive URL covers the gap.
4. **Geocoding accuracy** — NULL coords, coords outside the state bbox, and notices
   carrying a street address that should be street-level geocoded.

Plus the recurring failure modes (the `flags` column / `audit.py`): superseded
duplicates, estimated-vs-real `effective_date`, dead source links, HQ-vs-worksite
address, silent row-count drift, scraper failures, date/count sanity, low
enrichment.

## Legend

**Status**

| Symbol | Meaning |
|--------|---------|
| complete | No gap flags (a lone `has_superseded` note still counts complete). |
| gaps | One or more data-completeness flags (geo, pdf, dates, years, enrichment). |
| broken | Scraper failing, dead links, or silent row-count drift. |
| blocked / no data | Source unavailable, or no rows in DB. |

**Flags** (emitted by `warn-v2 audit`)

| Flag | Meaning | Remediation |
|------|---------|-------------|
| `low_geo` | <95% of notices geocoded | `backfill-geo` / `backfill-geo --rerun-address --state XX` |
| `out_of_state_coords` | geocoded point outside state bbox | fix geocode / verify worksite vs HQ address |
| `missing_pdf` | <90% PDF coverage on a PDF state | `download-pdfs --state XX` |
| `no_effective_date` | <50% have an effective_date | `backfill-effective-dates --state XX` |
| `mostly_estimated_dates` | >50% of effective_dates are the notice+60d estimate | source detail/PDF for real dates |
| `year_gaps` | empty interior year(s) | `backfill-historical --state XX` (extend `_SUPPORTED` if new) |
| `has_superseded` | superseded duplicates present | `mark-superseded --state XX --dry-run` then commit |
| `low_enrichment` | <50% of notices have an enriched company | `enrich --state XX` |
| `dead_links` | sampled `raw_notice_url`s 404 | fix scraper URL pattern (new code) |
| `row_drift` | last OK run below `expected_row_range` min | `/heal-scraper XX` (silent partial parse) |
| `scraper_*` | last run status not `ok` | `/heal-scraper XX` |
| `date_sanity` | future notice_date or effective<notice | inspect parser date handling |
| `count_outliers` | layoff_count <=0 or >50,000 | inspect parser count handling |
| `blocked` / `no_data` | known-blocked source or empty | re-check source access |

## Per-state table

> _Generated by `warn-v2 audit --markdown` (in-cluster, prod `DATABASE_URL` via
> secretKeyRef). Do not populate from ad-hoc `kubectl cp`/`exec` output — generate
> it from a trusted run._

<!-- BEGIN GENERATED TABLE -->
_Generated 2026-06-29 from prod via `warn-v2 audit --markdown`
(image `20260629-134906-3c8fcb3`)._

| State | Active | Superseded | Years | Eff% | Count% | PDF% | Geo% | Enrich% | Scraper | Status | Next action |
|-------|-------:|-----------:|-------|-----:|-------:|-----:|-----:|--------:|---------|--------|-------------|
| AK | 64 | 0 | 2006-2025 | 100% | 97% | 100% | 78% | 22% | ok | gaps | backfill-historical --state AK; backfill-geo; enrich --state AK; inspect parser date handling |
| AL | 1035 | 0 | 1998-2026 | 100% | 100% | - | 96% | 10% | ok | gaps | enrich --state AL; inspect parser date handling |
| AZ | 248 | 0 | 2016-2026 | 100% | 100% | n/a | 79% | 19% | ok | gaps | source detail/PDF for real dates; backfill-geo; enrich --state AZ |
| CA | 15751 | 0 | 2008-2026 | 100% | 72% | - | 100% | 16% | ok | gaps | backfill-historical --state CA; enrich --state CA; inspect parser date handling |
| CO | 44 | 0 | 1957-2021 | 100% | 86% | - | 25% | 2% | ok | gaps | backfill-historical --state CO; backfill-geo; enrich --state CO; inspect parser date handling |
| CT | 285 | 0 | 2019-2026 | 100% | 30% | 100% | 95% | 11% | ok | gaps | source detail/PDF for real dates; backfill-geo; enrich --state CT; inspect parser date handling |
| DC | 141 | 0 | 2005-2026 | 100% | 56% | - | 100% | 14% | ok | gaps | backfill-historical --state DC; enrich --state DC; inspect parser date handling |
| DE | 41 | 0 | 2016-2026 | 100% | 100% | n/a | 22% | 15% | ok | gaps | backfill-historical --state DE; source detail/PDF for real dates; backfill-geo; enrich --state DE |
| FL | 2294 | 0 | 2020-2026 | 100% | 100% | 45% | 100% | 27% | ok | gaps | download-pdfs --state FL; enrich --state FL; inspect parser date handling; inspect parser count handling |
| GA | 262 | 0 | 2023-2026 | 100% | 100% | n/a | 66% | 21% | fetch_failed | broken | /heal-scraper GA; backfill-geo; enrich --state GA; inspect parser date handling; inspect parser count handling |
| HI | 418 | 0 | 2019-2026 | 100% | 45% | 100% | 55% | 11% | ok | gaps | source detail/PDF for real dates; backfill-geo; enrich --state HI; inspect parser date handling |
| IA | 490 | 479 | 2021-2026 | 100% | 100% | - | 100% | 35% | ok | gaps | enrich --state IA; mark-superseded --state IA; inspect parser date handling |
| ID | 193 | 0 | 2009-2026 | 100% | 99% | - | 94% | 13% | ok | gaps | backfill-geo; enrich --state ID; inspect parser date handling |
| IL | 1025 | 14 | 2018-2026 | 100% | 15% | - | 99% | 15% | ok | gaps | enrich --state IL; mark-superseded --state IL; inspect parser date handling; inspect parser count handling |
| IN | 1010 | 0 | 2008-2026 | 100% | 98% | 99% | 92% | 13% | ok | gaps | backfill-geo; enrich --state IN; inspect parser date handling; inspect parser count handling |
| KS | 549 | 0 | 1999-2026 | 100% | 100% | n/a | 14% | 38% | ok | gaps | source detail/PDF for real dates; backfill-geo; enrich --state KS; inspect parser count handling |
| KY | 77 | 0 | 2025-2026 | 100% | 99% | 0% | 77% | 8% | ok | gaps | download-pdfs --state KY; backfill-geo; enrich --state KY; inspect parser date handling |
| LA | 5 | 0 | 2025-2026 | 100% | 100% | - | 100% | 20% | ok | gaps | enrich --state LA |
| MA | 86 | 0 | 2025-2026 | 100% | 100% | - | 72% | 6% | fetch_failed | broken | /heal-scraper MA; backfill-geo; enrich --state MA; inspect parser date handling |
| MD | 1329 | 0 | 2010-2026 | 100% | 97% | - | 96% | 20% | ok | gaps | enrich --state MD; inspect parser date handling; inspect parser count handling |
| ME | 79 | 0 | 2012-2026 | 100% | 100% | n/a | 52% | 19% | ok | gaps | source detail/PDF for real dates; backfill-geo; enrich --state ME; inspect parser count handling |
| MI | 104 | 0 | 2024-2026 | 100% | 95% | - | 90% | 13% | ok | gaps | backfill-geo; enrich --state MI |
| MN | 75 | 0 | 2023-2025 | 100% | 100% | - | 91% | 0% | ok | gaps | backfill-geo; enrich --state MN; inspect parser date handling |
| MO | 321 | 0 | 2019-2026 | 100% | 99% | - | 69% | 17% | ok | gaps | backfill-geo; enrich --state MO; inspect parser date handling |
| MS | 119 | 0 | 2013-2026 | 100% | 94% | - | 76% | 10% | ok | gaps | backfill-historical --state MS; backfill-geo; enrich --state MS; inspect parser date handling |
| MT | 43 | 0 | 2015-2026 | 100% | 91% | - | 60% | 5% | ok | gaps | backfill-historical --state MT; backfill-geo; enrich --state MT; inspect parser date handling |
| NC | 47 | 0 | 2026-2026 | 100% | 100% | - | 94% | 17% | ok | gaps | backfill-geo; enrich --state NC |
| ND | 54 | 0 | 2015-2026 | 100% | 100% | - | 80% | 15% | ok | gaps | backfill-historical --state ND; backfill-geo; enrich --state ND; inspect parser date handling |
| NE | 45 | 0 | 2023-2026 | 100% | 96% | 100% | 93% | 29% | ok | gaps | source detail/PDF for real dates; backfill-geo; enrich --state NE |
| NJ | 73 | 0 | 2026-2026 | 99% | 99% | - | 63% | 21% | ok | gaps | backfill-geo; enrich --state NJ; inspect parser date handling |
| NM | 114 | 0 | 2016-2026 | 100% | 97% | - | 96% | 24% | ok | gaps | enrich --state NM; inspect parser date handling |
| NV | 11 | 0 | 2025-2026 | 100% | 100% | - | 82% | 27% | ok | gaps | backfill-geo; enrich --state NV; inspect parser date handling |
| NY | 169 | 0 | 2025-2026 | 100% | 68% | 100% | 96% | 12% | ok | gaps | enrich --state NY; inspect parser count handling |
| OH | 53 | 0 | 2026-2026 | 100% | 100% | 100% | 91% | 8% | ok | gaps | backfill-geo; enrich --state OH; inspect parser date handling |
| OR | 100 | 0 | 2020-2026 | 100% | 98% | 0% | 100% | 16% | ok | gaps | source detail/PDF for real dates; download-pdfs --state OR; enrich --state OR |
| PA | 389 | 286 | 2024-2026 | 100% | 61% | - | 76% | 18% | ok | gaps | backfill-geo; enrich --state PA; mark-superseded --state PA; inspect parser date handling |
| RI | 125 | 0 | 2009-2026 | 100% | 98% | - | 54% | 18% | ok | gaps | backfill-geo; enrich --state RI; inspect parser date handling |
| SC | 24 | 0 | 2026-2026 | 100% | 100% | - | 96% | 38% | ok | gaps | enrich --state SC |
| SD | 78 | 0 | 2007-2026 | 100% | 99% | 91% | 97% | 14% | fetch_failed | broken | /heal-scraper SD; source detail/PDF for real dates; enrich --state SD; inspect parser date handling |
| TN | 90 | 0 | 2025-2026 | 100% | 99% | 99% | 100% | 21% | ok | gaps | enrich --state TN; inspect parser date handling |
| TX | 2238 | 0 | 2020-2026 | 100% | 100% | - | 100% | 6% | ok | gaps | enrich --state TX; inspect parser date handling |
| UT | 9 | 0 | 2026-2026 | 100% | 100% | - | 78% | 0% | ok | gaps | source detail/PDF for real dates; backfill-geo; enrich --state UT |
| VA | 1113 | 0 | 2010-2026 | 100% | 100% | 100% | 86% | 4% | ok | gaps | backfill-geo; enrich --state VA; inspect parser date handling; inspect parser count handling |
| VT | 97 | 0 | 2003-2026 | 100% | 100% | n/a | 30% | 9% | ok | gaps | backfill-historical --state VT; source detail/PDF for real dates; backfill-geo; enrich --state VT |
| WA | 23 | 0 | 2026-2026 | 100% | 100% | - | 65% | 35% | ok | gaps | backfill-geo; enrich --state WA |
| WI | 942 | 0 | 2016-2026 | 100% | 100% | 100% | 97% | 17% | ok | gaps | enrich --state WI; inspect parser date handling; inspect parser count handling |
| WV | 51 | 0 | 2021-2026 | 100% | 22% | 100% | 75% | 6% | ok | gaps | source detail/PDF for real dates; backfill-geo; enrich --state WV |
<!-- END GENERATED TABLE -->

## Geocoding root cause (investigated 2026-06-11)

**`backfill-geo` is exhausted, and the low-geo states are not fixable by it.** The
nightly `backfill-geo` CronJob is healthy and runs (08:47 daily), but its last run
filled **0 of 517** remaining NULL-coord locations — those carry junk/unresolvable
addresses (e.g. PA "Various locations") with no ZIP/city centroid fallback.

Critically, the worst low-geo states' gap is **notices with no `Location` row at
all** — which `backfill-geo` (it operates on existing `Location` rows) can never
touch. Per-state breakdown of active notices lacking a location:

| State | Active | No location row | Root cause |
|-------|-------:|----------------:|-----------|
| DC | 141 | 141 | Source table has no city/ZIP/address column; all notices are in Washington, DC. |
| MN | 75 | 70 | Source has an "Account: City" column, but only the 2026 clean-table parser captures it; the historical 2025 wide/merged-cell format can't split "Name City Industry". |
| WV | 51 | 49 | Listing page is employer+date only; per-notice PDFs are stored but yield no parseable worksite address. |
| HI | 12 | 11 | Source page publishes no city/county/ZIP. |
| DE | 41 | 29 | Mostly no locality at source. |
| CO | 44 | 31 | A handful carry a bare street fragment on the notice (no city/ZIP). |

Cluster-wide there are only **87** active notices that have a `Notice.address` but no
`Location` row (PA 79, CO 5, CT 3), and **0 of the 87 contain a 5-digit ZIP** — the
PA 79 are descriptive multi-site strings ("Two locations in Breinigsville, PA…"),
not single geocodable points. So a "geocode the notice address" backfill would
recover only a few messy CO/CT rows — low value.

**Status of each (2026-06-11):**
- **DC — FIXED** (geo 0% → 100%). `storage._derive_location_city` defaults the
  *location* city to "Washington" (the source guarantees all are in-District). It
  derives the city for the Location only, so `notice_id` (hashed from the original
  row) is unchanged — existing rows fill in `location_id` on the next scrape via
  COALESCE; no re-key. Existing 141 backfilled.
- **MN — FIXED** (geo 4% → ~87%). `scrapers/mn_city.split_city_from_label` recovers
  the worksite city from the wide-format `Name City Industry` string (strip the DEED
  industry label, then longest trailing gazetteer match), wired through the same
  `_derive_location_city` hook. 65/70 backfilled; the 5 misses are out-of-state HQ
  cities the source lists (Bentonville, San Francisco, Omaha, …) or a truncated
  city — correctly left un-geocoded.
- **WV — FIXED via OCR** (geo 4% → 75%). **HI — FIXED via OCR** (geo 8% → 50%).
  Both states' per-notice PDFs are scanned images with no text layer, so
  `pdf_extract` now falls back to OCR (tesseract + poppler in the image) when
  `pdfplumber` yields no text. WARN letters address state officials, so naive
  extraction pins the recipient (WV → the WorkForce WV DOL at "Charleston, WV
  25305"; HI → the DLIR at "Honolulu, HI 96813"); `_choose_city_zip` excludes
  recipient-agency ZIPs + recipient-marker contexts (Rapid Response, Dislocated
  Worker, Director, Bldg/Room, PO Box), prefers worksite-cued matches, and takes
  the most-frequent in-state survivor — else leaves the notice un-geocoded rather
  than placing a false capital/HQ pin. Backfilled: WV 38/51, HI 6/12 to real
  worksites; the remainder are recipient-only letters with no parseable worksite.

## Source notes (hand-curated)

Findings the DB can't tell us — confirmed against the live sources.

- **Missing/zero `layoff_count` investigation (2026-07-02)** — every state with
  Count% < 100 in the 2026-06-29 table was classified against its live source:
  - **CA — already healed.** The 2026-06-29 table's 72% was captured just before
    PR #97 (header variant + worksite sum) deployed; the cumulative EDD XLSX
    re-parse COALESCE-filled ~4,400 counts. Residual today: **18** rows, almost
    all 2020 COVID-era, blank at source.
  - **IL — real parser gap, fixed** (PR #105): archive monthly Excels
    (2020→mid-2025) title the column `# WORKERS AFFECTED:`; exact-match lookup
    dropped 871/1,025 counts the files actually contain. **Post-deploy: re-run
    `backfill-historical --state IL`** to heal prod.
  - **DC — real parser gap, fixed** (PR #106): the 2020 year page spells out
    "Number to Employees Affected" (alias missing) and uses comma-thousands
    ("1,604") that `as_int` rejected — 52/52 of the 2020 backfill batch were
    dropped. **Post-deploy: re-run `backfill-historical --state DC`.**
  - **PA — mostly source-side, partial fix** (PR #108): ~14 of 150 null rows
    carry free-text counts ("5 (within PA)", "501 @ Etters; 595 @ Philadelphia")
    now extracted; the rest publish no usable number (no `# AFFECTED` label,
    unknown/TBD, or non-first location blocks). Heals on nightly scrape.
  - **NY — legacy rows, not a current-parser gap**: all 54 nulls predate the
    Apr-2025 Tableau-dashboard cutover (old `dol.ny.gov/warn-…` detail-page
    rows); 2026 rows are 100% filled. The old per-notice pages still resolve and
    show the count — a one-shot backfill from `raw_notice_url` would close it.
  - **CT / HI / WV — expected**: the listings publish no counts (CT: PDF blob
    library; HI: PDF-link paragraphs; WV: employer+date anchors). Counts exist
    only inside the stored per-notice PDFs — extraction via the existing
    OCR/pdf_extract pipeline is the enhancement that would close these.
    Addressed by `backfill-layoff-counts` (PR #113): conservative fill-only
    extraction from the stored PDFs. **Post-deploy: run the one-off in-cluster
    `backfill-layoff-counts` Job** (dry-run first); letters stating no total
    (e.g. "all employees" unquantified, redacted tables) correctly stay NULL.
  - **Zero counts (`count_outliers`) are genuine source zeros**, not parser
    artifacts: rescissions (NY "(Rescission)", VA "*notice rescinded") and
    0-listed filings (FL/IN Yellow Corporation 2023, MD Capital One 2020).
    No >50k outliers exist in the current data.
  - **Small tails elsewhere** (MD 43, IN 21, MS 7, CO 6, MI 5, WI 4, MT 4, AK 2,
    NE 2, NJ 1): blank/TBD cells at source — spot-checked, not systemic.

- **Scraper health at this run (2026-06-29)** — GA, MA, and SD reported
  `fetch_failed` (status `broken`). `fetch_failed` is often a transient source
  block rather than a parser break (see `/heal-scraper` classification); run
  `/heal-scraper GA MA SD` to confirm whether any is a real break before treating
  it as one. GA in particular had previously recovered to `ok` (see the GA note
  below), so its reappearance here is most likely the TCSG rate-limit/timeout, not
  a regression.
- **Backfills landed since the 2026-06-11 table** (now reflected above): KS
  1999–2026 (549), ME 2012–2026 (79), VT 2003–2026 (97), plus large historical
  pulls for FL (2294), TX (2238), MD (1329), HI (418), IL (1025), MS (119), and
  NM (114). TN is now live (90 notices, 2025–2026) after the 2026-06-26 unblock.
  Several big backfills brought in location-less rows, so geo% dropped where the
  new history lacks geocodable worksites — notably KS (14%) and VT (30%).
- **Historical backfill routes** — per-state classification (published archive vs
  public-records request) lives in [docs/historical-sources.md](docs/historical-sources.md),
  verified 2026-06-12. Records-request drafts (18 states) in [docs/foia/](docs/foia/).
- **AK** — the stale-2016-link 404 issue is **fixed** (the scraper uses
  `jobs.alaska.gov/RR/notices/…`; verified 2026-06-11: stored `raw_notice_url`s
  and live source anchors all return 200, 49/64 notices have downloaded PDFs).
  AK's residual is a geo tail (66%): older notices without a PDF link + remote
  AK localities not in the city gazetteer — a hard tail, not a scraper bug.
  The interior **year gaps (2009, 2011, 2014) are real** — the cumulative source
  page lists zero notices for those years (verified 2026-06-12); no backfill needed.
- **CO** — Google Sheets export is cumulative since 2019; excluded from
  `backfill-historical` by design (re-ingest would duplicate).
- **CA / DC / AZ / DE** — current-year-only sources; historical years come from
  `backfill-historical`. CA archive = 11 PDFs + 1 XLSX (FY2014–2025).
- **GA** — `raw_notice_url` is a TCSG GravityView HTML entry page, not a PDF
  (`raw_notice_url_is_pdf=False`); enriched by `enrich-notices --state GA`. TCSG
  **rate-limits aggressively**: the enricher aborts after 3 consecutive timeouts, so
  a tail of notices stays location-less. **Hardened 2026-06-11** so this clears over
  nightly runs *without* hammering TCSG: commits every 5 (was 50) so each run durably
  banks what it fetched before the block; an unexpected error banks progress instead
  of crashing the Job. **2026-06-17**: `closure_type` reached 100%, which collapsed the
  old `closure_type IS NULL` ordering to deterministic newest-first — the same ~15-18
  notices were re-fetched every run (their gk-download is a Word/Excel doc, not a PDF,
  so they never left the candidate set) and starved the rest. Fixed with a per-notice
  `attachment_fetched_at` stamp (candidate query keys off it; one fetch per notice +
  timeout retries), and the gk-download attachment is now extracted for any type —
  the `.docx` is the WARN letter in full text (worksite address, dates, counts), the
  `.xlsx` is usually a job-title roster. `Company Address` on the entry page is often
  the corporate HQ, not the GA worksite → Census no-match / out-of-state coords, so the
  attachment's worksite is preferred when present. (Worksite city/zip extraction needs
  the 2-letter state form; letters that spell out "Georgia" still rely on the page's
  Zip Code/County — a `_choose_city_zip` full-state-name follow-up would close that.)
- **KY / MT** — county-only sources; geocoded via Tier-4 county centroid.
- **`inspect parser date handling`** appears widely — driven by the estimated-date
  heuristic (effective_date = notice_date + 60d). That's the expected WARN-Act
  fallback for many states, not necessarily a bug; treat as "real date unavailable
  at source" unless the source actually provides one.
- **Previously-flagged bugs now fixed** (confirmed in this table): MT/RI corrupt
  `notice_date` years (`225`/`204`) — fixed in place + `as_date` year-bounds guard;
  MT now 2015-2026, RI 2009-2026. GA scraper `fetch_failed` was fixed to `ok`
  (it shows `fetch_failed` again at the 2026-06-29 run — see the scraper-health
  note at the top of this section).
- **Blocked (not registered scrapers; appear in the audit only if legacy rows
  exist):** AR (confidential by law), NH (no public listing), OK
  (Salesforce/Aura auth wall), WY (no public data). Re-verify each blocker
  periodically. (TN was unblocked 2026-06-26 via `curl_cffi` TLS impersonation.)

## Backlog (genuinely new code, not just a command)

- ~~**DC geo**~~ — DONE (storage `_derive_location_city`; 0% → 100%).
- ~~**MN geo**~~ — DONE (`scrapers/mn_city`; 4% → ~87%).
- ~~**GA tail**~~ — DONE (durable commits + never-enriched-first ordering; clears
  over nightly runs).
- ~~**WV / HI geo**~~ — DONE via OCR fallback (tesseract) + recipient-aware
  address selection (WV 4%→75%, HI 8%→50%; recipient-only letters stay unlocated).
- ~~**AK** URL-pattern fix~~ — already fixed (`jobs.alaska.gov`); residual is a
  geo tail (remote localities / PDF-less older notices), not a scraper bug.
- **Historical backfill expansion** — extend `backfill-historical` beyond
  CA/DC/AZ/DE per the tiered plan in
  [docs/historical-sources.md](docs/historical-sources.md) (JobLink freebies
  KS/ME/VT → year-URL loops TX/FL/NC/NJ/NM/HI/KY/MO → new parsers OH 1996+,
  PA 2001+, IL 2002+, NY/MD/WI/MN/MS → WA pagination fix); records-request
  responses ingested via a new `ingest-file` command. Email drafts ready in
  [docs/foia/](docs/foia/).
- Any state the audit flags `dead_links`, `row_drift`, or persistent `scraper_*`.
- Bulk company enrichment is low (0–18%) almost everywhere; the D&B + EDGAR + Claude
  enricher runs nightly (25/run, 100/day) and climbs over time.

## Optional enhancement

`Location` now has a `geocode_source` column (`census`/`zip`/`city`/`county`), so
the audit could read the stored tier directly instead of inferring geo accuracy
from address availability + coord presence. Wiring the audit to use it would make
geo-accuracy reporting exact and re-runnable.
